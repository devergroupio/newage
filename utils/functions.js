export const errorSomething = () => {
  // eslint-disable-next-line no-alert
  alert('Something went wrong, please try again');
};

export const errorQRNotElle = () => {
  // eslint-disable-next-line no-alert
  alert('Please get scan the QR code of Elle nail store');
};

export const errorWithText = (text) => {
  // eslint-disable-next-line no-alert
  alert(text);
};
